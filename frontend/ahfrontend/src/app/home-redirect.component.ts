import { Component } from '@angular/core';

/** Placeholder for routes that only exist to run roleDefaultGuard (guard returns UrlTree before this renders). */
@Component({
  selector: 'app-home-redirect',
  standalone: true,
  template: '',
})
export class HomeRedirectComponent {}
